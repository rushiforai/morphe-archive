org.gradle.jvmargs = -Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching = true
# GitHub PAT pour accéder au registry Morphe (scope: read:packages)
# Remplace les valeurs ci-dessous avec tes vraies credentials GitHub
# ou définis-les dans ~/.gradle/gradle.properties pour ne pas les committer.
# gpr.user=TON_USERNAME_GITHUB
# gpr.key=TON_GITHUB_PAT_TOKEN
version = 1.0.0
