package android.util;

/**
 * Compile-time shape only — an empty marker; see android/content/Context.java in this module for
 * why an android type lives in {@code :stubs} at all.
 */
public interface AttributeSet {
}
