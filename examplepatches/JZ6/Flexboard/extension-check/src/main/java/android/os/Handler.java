package android.os;

/**
 * Compile-time shape only — the settings fragment posts its open-time paint through one.
 */
public class Handler {

    public Handler(Looper looper) {
        // stub
    }

    public boolean post(Runnable action) {
        return false;
    }
}
