rootProject.name = "anxy-patches"

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

include(":patches")
include(":extensions:alightmotion")
