// Compile-time stub ONLY. Not shipped in the dex.
// The real com.ather.maps.a0 enum lives in the app; this stub exists so
// MapPref.java can reference a0.MMI / a0.GOOGLE_MAP / a0.OSM at compile time.
// d8 dexes MapPref alone, leaving symbolic references that resolve at runtime.
package com.ather.maps;

public enum a0 {
    MMI,
    GOOGLE_MAP,
    OSM
}
