package de.robv.android.xposed;

/**
 * Compile-only stub mirroring the legacy Xposed API shipped by LSPosed 1.9.x
 * (core/src/main/java/de/robv/android/xposed/IXposedMod.java). Signatures must
 * match the runtime exactly; never bundle these classes into the module APK.
 */
public interface IXposedMod {
}
