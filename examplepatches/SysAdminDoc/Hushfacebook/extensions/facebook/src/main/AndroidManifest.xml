<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Nothing here is merged into Facebook; the patches add what the app needs. -->
    <application android:supportsRtl="true" />
</manifest>
