org.gradle.parallel = true
org.gradle.caching = true
kotlin.code.style = official
version = 1.3.1
# GitHub Packages credentials for the Morphe registry.
# Do NOT hardcode your token here — use the GITHUB_ACTOR and GITHUB_TOKEN
# environment variables instead (already wired up in settings.gradle.kts).
# gpr.user = your-username
# gpr.key = your-token
