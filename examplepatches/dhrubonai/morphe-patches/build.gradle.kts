// Intentionally empty: subprojects are wired by the app.morphe.patches settings
// plugin. Dependabot's gradle updater refuses to scan a repo without a root
// build file, so this exists to let it read gradle/libs.versions.toml.
