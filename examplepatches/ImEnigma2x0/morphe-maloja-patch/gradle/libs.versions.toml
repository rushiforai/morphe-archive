[versions]
morphe-patcher = "1.13.0"
# Must match the version bundled by the official Morphe patches, since the extension
# compiles against it and resolves it at runtime from the official shared extension.
morphe-patches-library = "1.6.2"
#noinspection GradleDependency
smali = "d92701d947"
gson = "2.14.0"

[libraries]
gson = { module = "com.google.code.gson:gson", version.ref = "gson" }
morphe-extensions-library = { module = "app.morphe:morphe-extensions-library", version.ref = "morphe-patches-library" }
# Gradle plugins adds the dependency during build, this declaration is here only for dependabot to provide updates.
#noinspection UnusedVersionCatalogEntry
morphe-patcher = { module = "app.morphe:morphe-patcher", version.ref = "morphe-patcher" }
