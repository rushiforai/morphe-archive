package app.braydog2010.extension.extension;

@SuppressWarnings("unused")
public class VenaboxHubExtension {
    // Java code placed here runs inside the patched app's process at runtime.
    // Use this for logic that must execute in the app's context (e.g. hooking into live state).
    // Simple patches that override bytecode with fixed values do not need extension code.
}
