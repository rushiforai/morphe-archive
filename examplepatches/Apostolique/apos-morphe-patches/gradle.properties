org.gradle.jvmargs=-Xmx2048m
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official

# Gradle needs a JDK 17 or newer for the Android plugin. CI pins one; locally
# set JAVA_HOME, or add org.gradle.java.home to ~/.gradle/gradle.properties so
# the path stays out of this repository.

# The bundle version Morphe Manager shows. Written by semantic-release on every
# release, so edit the commit message rather than this line.
version=1.0.0
