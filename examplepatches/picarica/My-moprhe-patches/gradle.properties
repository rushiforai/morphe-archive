org.gradle.parallel = true
org.gradle.caching = true
kotlin.code.style = official
version = 1.0.1

# Morphe's settings plugin requires these properties to exist even when the
# patcher and Gradle plugin are supplied by the pinned public source checkouts.
gpr.user = unused-local-source-build
gpr.key = unused-local-source-build
