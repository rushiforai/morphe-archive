name: Release

# Run manually from the Actions tab: pick a version, get a release Morphe
# Manager can consume as a patch source. What changed is written from the
# commits since the previous release, into the release's notes and into
# CHANGELOG.md, which is where Manager reads it from.
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release, e.g. 1.0.0"
        required: true
        default: "1.0.0"

jobs:
  release:
    name: Build and release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      # Needed to read the Morphe Gradle plugin from MorpheApp's package registry.
      packages: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # The whole history and its tags, so the release notes can list the
          # commits since the previous release.
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Build the patch bundle
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ inputs.version }}
        run: |
          chmod +x gradlew
          ./gradlew buildAndroid generatePatchesList -Pversion="$VERSION" --no-daemon --console=plain

      # Before the release commit below, so that commit is not in its own notes.
      - name: Write the release notes
        run: python3 .github/scripts/release_notes.py > "$RUNNER_TEMP/notes.md"

      # Morphe Manager reads these two files straight from the repo to show the
      # bundle and its patches before downloading anything.
      - name: Update bundle metadata, README and CHANGELOG
        env:
          VERSION: ${{ inputs.version }}
        run: |
          jq --arg version "$VERSION" '.version = $version' patches-list.json > patches-list.json.tmp
          mv patches-list.json.tmp patches-list.json

          # The patches list in README.md is generated from patches-list.json.
          python3 .github/scripts/generate_patches_readme.py

          # Manager shows a bundle's changes from CHANGELOG.md, never from a release.
          python3 .github/scripts/release_notes.py --changelog "$VERSION"

          DOWNLOAD_URL="https://github.com/${GITHUB_REPOSITORY}/releases/download/v${VERSION}/patches-${VERSION}.mpp"
          jq --arg version "$VERSION" \
             --arg url "$DOWNLOAD_URL" \
             --arg created "$(date -u +%Y-%m-%dT%H:%M:%S)" \
             '.version = $version | .download_url = $url | .created_at = $created' \
             patches-bundle.json > patches-bundle.json.tmp
          mv patches-bundle.json.tmp patches-bundle.json

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add patches-bundle.json patches-list.json README.md CHANGELOG.md
          git commit -m "chore: release v${VERSION} [skip ci]"
          git push

      - name: Create the release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ inputs.version }}
        run: |
          gh release create "v${VERSION}" \
            "patches/build/libs/patches-${VERSION}.mpp" \
            --title "v${VERSION}" \
            --notes-file "$RUNNER_TEMP/notes.md"
