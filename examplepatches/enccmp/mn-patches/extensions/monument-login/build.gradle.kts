extension {
    // Path the bytecode patch references via extendWith("extensions/monument-login.mpe").
    name = "extensions/monument-login.mpe"
}

android {
    namespace = "app.morphe.extension.monument"
}
