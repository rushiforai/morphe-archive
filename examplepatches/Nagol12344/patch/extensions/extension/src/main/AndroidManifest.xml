<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed for the charityNative fetch bridge when the extension runs as
         a standalone app; harmless when merged into the bplace app (it
         already declares INTERNET). -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>