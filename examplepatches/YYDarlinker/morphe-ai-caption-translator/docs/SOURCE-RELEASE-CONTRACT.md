# Source release contract — independent project

## Package identity

- Display name: Anchored AI Captions
- Repository: YYDarlinker/morphe-ai-caption-translator
- Gradle root / release package: anchored-ai-captions
- Runtime retains its typed adapter namespace; it does not bundle official Morphe extension classes.
- Source ancestry and GPL/NOTICE remain. Upstream development release notes and disconnected experiments are not shipped.

## Manager contract checked against official source

Reference: MorpheApp/morphe-manager main commit ca22fa9f121a323e1eb2780b5cf75218faa06619.
`network/dto/MorpheAsset.kt` expects `created_at` as kotlinx.datetime.LocalDateTime (no Z/offset), `download_url`, `description`, `version`; signature/page URL fields are optional. `domain/bundles/RemotePatchBundle.kt` uses main/dev manifests. A GitHub release alone is insufficient for a JSON-based source.

## September 13, 2026 repair

The prior delivery mistakenly retained another project's manifest and timestamp, an incorrect package Source URL, and a non-executable Gradle wrapper in Git. The first automatic publication succeeded but its follow-up backmerge failed because dev did not exist. Both branches now exist and point at the new project's release metadata. The old manually published dev asset is superseded, not silently replaced again.

The ordinary source channel resolves to 1.0.0. This is a distribution-channel designation, **not evidence of phone runtime or translation-quality acceptance**.

## Independent verification

- 16 JVM caption regression tests passed; no ignored tests.
- 5 release-contract tests passed (including rejection of stale repository, mismatched version, timezone suffix, missing URL).
- Anonymous main and dev JSON fetches: HTTP 200, version 1.0.0, one patch.
- Manifest-referenced artifact: HTTP 200, 887217 bytes.
- SHA-256: b2197048ddc795efd3a5815a8f7f9411b1444dd9aaa2c197e709c58011476d0c.
- MPP ZIP CRC passes, root classes.dex is present, embedded extension.mpe is valid raw DEX (not a nested ZIP).
- Manifest display name/version/source agree; retired ledger/page engines and target-length splitter are absent from extension DEX.
- Official desktop CLI successfully loaded the repository remotely and enumerated AI caption translator.
- The downloaded published artifact patched/rebuilt original YouTube 21.07.247 with official 1.42.0 Captions: no failed patches.
- Independent local candidate patched/rebuilt official default combination. Candidate root DEX and extension DEX are byte-identical to published 1.0.0.
- No Android device was connected. Manager on-phone source card, playback/UI and model output quality are not device-verified.

## Prevention

Release validation is a required pipeline step; generated timestamps are normalized before validation. Android DEX packaging, namespace/identity, anonymous source fetch and referenced artifact are checked. Future versions are generated by semantic-release and the Morphe changelog plugin, not manual asset replacement. Stable-to-dev synchronization follows the template workflow.
